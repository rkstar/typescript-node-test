import * as React from 'react'

interface AppProps {}
class App extends React.Component<AppProps, undefined>{
  render(){
    return (
      <h1>... and now we can start developing.</h1>
    )
  }
}

export default App